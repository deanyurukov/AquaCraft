{
    "version": 2,
    "builds": [
      {
        "src": "server/src/api/*.js",
        "use": "@vercel/node"
      }
    ],
    "routes": [
      {
        "src": "/server/src/api/(.*)",
        "dest": "/server/src/api/$1"
      }
    ]
  }